{
  "name": "dynamic-acquisition-platform",
  "private": true,
  "version": "0.0.1",
  "description": "Incubation project for shared browser-assisted content acquisition contracts, discovery, planning, and runtime primitives.",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test",
    "lint": "echo \"lint not configured yet\"",
    "check": "npm run typecheck && node scripts/check-contracts.js"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
